重新梳理一下逻辑。

** 使用滑动窗口协议单独写一个保存文件的接口。

1、用户调用coapmessage发送消息， 包含
公有 消息的发送、接收者；消息的内容；回调函数
包内可用 消息的uuid

2、消息的拆分类，包含
包内可用 消息的uuid；用户填写的消息体；接收者；
一个装碎片的list（或环形数组）（方便对方收到后删除）；碎片的总数；
当前对方收到的碎片数；
方法： 拆分；

3、消息的合并类， 包含
包内可用 消息的uuid；还原出的消息体；发送者；
一个装碎片的数组（方便收到后组装）;碎片的总数；
当前收到的碎片数；